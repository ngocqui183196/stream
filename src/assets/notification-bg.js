 // self.registration.showNotification(notificationTitle, notificationOptions)

 const notificationBg = (notificationTitle, notificationOptions) => {
     console.log(notificationTitle + '-notification-bg.js', notificationOptions)
    // self.registration.showNotification(notificationTitle, notificationOptions)
}
